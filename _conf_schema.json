{
  "repeat_threshold": {
    "description": "复读阈值",
    "type": "int",
    "default": 3,
    "hint": "当不同群友连续发送相同消息的次数达到此值时，机器人将复读该消息（最小为2）。设为3表示需要3条相同消息才触发复读。"
  },
  "cooldown_seconds": {
    "description": "同内容复读冷却时间（秒）",
    "type": "int",
    "default": 300,
    "hint": "相同内容被复读后，在此冷却时间内不会再次复读该内容。设为300表示5分钟内同一内容只复读一次。"
  }
}
